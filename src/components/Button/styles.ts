import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "#3B6EDC",
    padding: 14,
    borderRadius: 10,
    alignItems: "center",
  },
  disabled: {
    backgroundColor: "#999",
  },
  text: {
    color: "#fff",
    fontWeight: "bold",
  },
});
